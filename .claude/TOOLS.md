# TOOLS.md - Local Notes

Skills define *how* tools work. This file is for *your* specifics — environment-specific stuff.

## 🔴 禁止 TODO — 铁律

不许写任何 TODO，除非客观原因无法实现。placeholder 代码 + TODO 注释 = 未完成。

## macOS 剪贴板

浏览器沙箱限制无法直接读取剪贴板。用系统命令：
```bash
pbpaste   # 读取
pbcopy    # 写入（echo "text" | pbcopy）
```

## Slack
已通过 OpenClaw plugin 配置，直接用 `message` tool。

## Creem Dashboard
- Credentials: `~/.config/creem/credentials`
- ⚠️ 尽量减少登录频率，避免触发验证

---

Add whatever helps you do your job. This is your cheat sheet.
